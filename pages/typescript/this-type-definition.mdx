# this type 정의

`this` 는 함수 내부에서 컨텍스트를 나타내는 예약어인데,

이것의 타입을 지정할 수 있다

```ts
function example(this: SomeType, arg: number) {
  this.someMethod();
}
```

this의 타입을 지정하지 않을 경우 `any` 로 추론되기에
IDE 에서 `this.` 를 시도했을 때 사용할 수 있는게 없다.

그래서 타입을 정의해주면 좋다